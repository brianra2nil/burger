<div class="jumbotron">
  <h1 class="display-4">Burger App</h1>
  <p class="lead"> An app to keep track of every burger you have devoured!</p>

</div>


<div class="container">
  <div class="row">
    <div class="col-md-12">
      <form>
        <div class="form-group">
          <label for="burger">Enter Burger Name</label>
          <input type="text" class="form-control" id="product">

        </div>

        <button type="submit" id="addBurger" class="btn btn-primary">Submit</button>
        <hr>
      </form>
    </div>
  </div>
  <div class="row">
    <div class="col-mid-6">

      <ul id="notDevoured" class="list-group">
        <h2>Not Devoured</h2>
        {{#each burgers}}
        {{#unless this.devoured}}
        <li id="{{this.id}}"class="list-group-item ">
          <div class="d-flex w-100 justify-content-between">
            <h5 class="mb-1">{{this.name}}</h5>
          <button 
          data-name="{{this.name}}"
          class="devour btn btn-success">Devour it!</button>
          </div>
          <p class="mb-1"></p>
          <small>Devoured? {{ this.devoured }}</small>
        </li>
        {{/unless}}
        {{/each}}
      </ul>

    </div>
    <div class="col-mid-6">
      <ul id="devoured" class="list-group">
        <h2>Devoured !</h2>
        {{#each burgers}}
        {{#if this.devoured}}
        <li id="{{this.id}}" class="list-group-item ">
          <div class="d-flex w-100 justify-content-between">
            <h5 class="mb-1">{{this.name}}</h5>

          </div>
          <p class="mb-1"></p>
          <small>Devoured? {{ this.devoured }}</small>
        </li>
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>